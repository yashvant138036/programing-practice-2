//----------------4. Enter N elements and find how many are positive, negative, even and odd.---------------------//
#include<stdio.h>
#include<string.h>


void main()
{
    char str[20];

    printf("enter string:");

    gets(str);
    puts(str);
}
